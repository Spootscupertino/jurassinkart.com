    <section class="hero">
    <!-- Parallax container needs overflow hidden which is set on .hero -->
    <div class="hero-bg parallax-layer">
      <!-- Fixed Video path: using Astro's best static usage or fixing typo -->
      <video autoplay loop muted playsinline preload="auto" poster="/assets/prehistoric-tree.png" aria-label="Jurassic jungle canopy background">
        <source src="/assets/forest-canopy-loop.mp4" type="video/mp4" />
      </video>
      <img class="hero-fallback" src="/assets/prehistoric-tree.png" alt="Prehistoric jungle canopy" loading="eager" decoding="async" />
      <div class="hero-overlay"></div>
    </div>
  </section>

  <section class="hero-intro">
    <div class="hero-content parallax-content">
      <h1>Step into a Dino day dream and a world of prehistoric wonder.</h1>
      <a class="btn magnetic-btn" href="https://www.etsy.com/shop/Jurassinkart" target="_blank" rel="noopener">Shop the Collection</a>
    </div>
  </section>

  <style>
    .hero-bg {
      position: absolute;
      top: -10%; left: -10%;
      width: 120%; height: 120%;
      will-change: transform;
    }
    .hero-bg video {
      position: absolute;
      inset: 0;
      width: 100%; height: 100%;
      object-fit: cover;
      opacity: 0.45;
    }
    .hero-fallback {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      opacity: 0.28;
      filter: saturate(0.9) brightness(0.8);
      z-index: -1;
    }
    .hero-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(
        to bottom,
        rgba(9,14,23,0.3) 0%,
        rgba(9,14,23,0.6) 60%,
        #090e17 100%
      );
    }
    /* Hide scrollbar / extra styles applied via index.astro */
  </style>
